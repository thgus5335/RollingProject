const Cardnation = ({ totalPages, cardCountPerPage, cardCount, currentCard }) => {};

//cardCountPerPage = 4
//cardCount = results.length
//totalPages = Math.ceil(cardCount/cardCountPerPage)

export default Cardnation;
